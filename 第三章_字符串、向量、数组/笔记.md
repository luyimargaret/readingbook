# 3.1 命名空间的using声明
头文件不应包含using声明
# 3.2 标准库类型string
直接初始化和拷贝初始化（使用=）
getline(cin,line);//读入一行，触发getline（）函数返回的换行符实际上被丢掉了，得到的line中不包含该换行符
C++11范围for（）语句不想改变字符就不加引用，改就加引用
# 3.3 标准库类型vector
# 3.4 迭代器介绍
当容器为空时，begin和end返回的是同一个迭代器，都是尾后迭代器
# 3.5 数组
# 3.6 多维数组

